// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remove the experimental section entirely
  // experimental: {
  //   appDir: true,
  // },
};

module.exports = nextConfig;
